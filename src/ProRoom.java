public abstract class ProRoom extends Room {
    public ProRoom(int number) {
        super(number);
    }

    public ProRoom(int number, int capacity, int price) {
        super(number, capacity, price);
    }
}
